var edad = 18;
if(edad === 18) {
    console.log("Puedes votar, será tu primera votación");
} else if (edad > 18) {
    console.log("Puedes volver a votar");
} else {
    console.log("aún no puedes votar");
}

condition ? true : false;

var numero = 1;

var resultado = numero === 1 ? "Sí soy un uno" : "No, no soy un uno";
//hay que mandarlo a llamar con console.log para para ver el resultado con la primera variable que se encuentra en la consición, por ejemplo

console.log(resultado)